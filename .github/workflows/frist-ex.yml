name: My first workflow
on: push
jobs:
    first_job:
        runs-on: ubuntu-latest
        steps:
        - name: welcome message
          run: echo My first GHA job"

        - name: list file
          run: ls

        - name: read ile
          run: cat README.md
